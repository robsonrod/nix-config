#!/usr/bin/env bash

main() {
	city=${1}
	if [ $# -lt 1 ]; then
		local selected
		selected=$(echo bsb$'\n'slz$'\n'gru | awk '{print}' | fzf)
		city="${selected}"
	fi

	local url="http://v3.wttr.in/$city"
	curl "${url}"
}

main "$@"
